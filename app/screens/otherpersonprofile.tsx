import { View, Text, SafeAreaView, ScrollView } from 'react-native'
import React from 'react'

export const OtherPersonProfile = () => {
  return (
    <SafeAreaView>
      <ScrollView>
        <View>
          <Text>OtherPersonProfile</Text>
        </View>
      </ScrollView>
    </SafeAreaView>
  );
}
